osx_image: beta-xcode6.3
language: objective-c
xcode_project: BingAPI.xcodeproj
xcode_sdk: iphonesimulator

xcode_scheme:
#- BingAPI
- "Podspec Lint"

script:
- xcodebuild test -project BingAPI.xcodeproj -scheme 'BingAPI' -destination 'platform=iOS Simulator,name=iPhone Retina (4-inch)'

env:
    global:
    - TIMEOUT=1000
    matrix:
    - USE_NETWORK=true
